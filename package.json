{
  "name": "leanmate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vercel dev",
    "check": "node -c api/members.js && node -c api/attendance.js && node -c api/nutrition-search.js"
  },
  "devDependencies": {
    "vercel": "^34.3.0"
  }
}
